#include<bits/stdc++.h>
using namespace std;

template<class T>
class Node{
   public:
      int data;
      Node<T>* next;
      Node(T x){
         data = x;
         next = NULL;
      }
};

Node* insert(Node* root,int x){
   if(root==NULL){
      return new Node*(x);;
   }
}

int main()
{
   Node* head = NULL;

   return 0;
}